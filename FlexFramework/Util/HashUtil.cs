using System.Security.Cryptography;

namespace FlexFramework.Util;

public static class HashUtil
{
    public static Hash256 Hash(ReadOnlySpan<byte> buffer)
    {
        // Compute hash
        
    }
}